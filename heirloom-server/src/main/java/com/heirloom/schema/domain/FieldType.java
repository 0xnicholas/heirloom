package com.heirloom.schema.domain;

/**
 * The six valid field types in a Resource Type definition.
 */
public enum FieldType {
    STRING,
    NUMBER,
    BOOLEAN,
    ENUM,
    DATE,
    TIMESTAMP,
    RID
}
